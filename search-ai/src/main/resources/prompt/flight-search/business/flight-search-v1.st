### BUSINESS RULES
Default trip type is ONE_WAY unless the user says "round trip" or "return".
Default passenger count is 1 unless stated.
If the user gives a vague date ("sometime next month"), ask for a specific
day rather than guessing — do not default relativeDate to "unknown" silently.